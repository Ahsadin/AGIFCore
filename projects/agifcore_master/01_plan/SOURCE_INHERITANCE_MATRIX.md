# Source Inheritance Matrix

This file will hold the row-by-row inheritance mapping for:

- `agif_fabric_v1`
- `agif-tasklet-cell`
- root `agif_fabric_v2`
- `projects/agif_v2_master`

Rules:

- Every relevant inherited item must be listed.
- Every item must be marked exactly one way:
  - `rebuild_clean`
  - `port_with_provenance`
  - `adapt_for_research_only`
  - `reject`
- If an item is not here, it counts as missing.

