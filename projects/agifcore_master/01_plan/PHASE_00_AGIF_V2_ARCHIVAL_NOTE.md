# Phase 0 AGIF v2 Archival Note

## Purpose

This file closes the missing project-level archival-note gap identified in the frozen Phase 1 handoff.

## Frozen archival statement

- `AGIF v2` is archived as a failed historical attempt for the intended AGIF end goal.
- The `root v2 lineage` and `agif_v2_master` remain source material only.
- No AGIF v2 code, proof, benchmark, demo, or report counts as AGIFCore progress by itself.
- Any reused idea from AGIF v2 must later appear in `01_plan/SOURCE_INHERITANCE_MATRIX.md` with an explicit disposition.

## Allowed use

- read the old repos for source mining
- compare old ideas against the frozen AGIFCore master plan
- port or rebuild only after provenance mapping, verification, demo, and explicit user approval

## Disallowed shortcut

- treating any AGIF v2 artifact as already earned inside AGIFCore
- skipping provenance because a similar v2 artifact exists
- claiming Phase 1 or later closure from inherited AGIF v2 material alone

## Boundary note

This archival note resolves the missing distinct archival-note artifact.
It does not approve Phase 0 or Phase 1 on its own.
